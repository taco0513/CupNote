# CupNote npm configuration
# This project uses npm as the package manager
# DO NOT use bun, yarn, or pnpm

# Ensure npm is used
engine-strict=true
package-lock=true

# Performance optimizations
prefer-offline=true
audit-level=high